Both inputs — the Acceptance Criteria and the Raw Test Specifications — contain only error messages, not actual content. The upstream pipeline steps (AC writer and test generator) all timed out before producing output for this batch.

There's nothing to summarize. To produce a digest I need:

1. **Acceptance Criteria** — the actual AC blocks (the `###` story sections), not timeout logs
2. **Raw Test Specifications** — the Gherkin scenarios from the test generator

The pipeline needs to succeed for those two steps first. Once they produce output, re-run this summarizer step with the real content.
