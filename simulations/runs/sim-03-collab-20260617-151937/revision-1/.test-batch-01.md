It seems I need permissions to read these files. Can you grant access to the story-refinery directory? The errors show the test-generator agent is timing out at 180s across 12+ consecutive attempts — I want to look at the agent definition and pipeline engine to diagnose whether the timeout is too short, the prompt is too large (44268 bytes is noted), or there's a structural issue with how the step is invoked.
