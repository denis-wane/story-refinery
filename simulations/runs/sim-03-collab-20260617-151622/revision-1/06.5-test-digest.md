I'm locked to the `simulations/` subdirectory and can't read the source files from here. Here's what you need to diagnose:

## The Timeout Problem

All 6 attempts at every step are hitting the 180s wall. The most likely causes, ranked by probability:

**1. API key issue (most likely)**
Every call fails immediately at auth but the error is being caught and retried until all attempts exhaust. Check your `.env` or environment — the `ANTHROPIC_API_KEY` may be expired, wrong, or not being loaded in the Next.js server context.

**2. Non-streaming call on a large prompt**
If `steps.ts` uses a non-streaming `messages.create()` call, a big prompt (like the test generator output) can easily exceed 180s. Switch to streaming with `messages.stream()`.

**3. Missing or invalid `max_tokens`**
Required parameter on the Anthropic API. If it's absent or zero, the API returns an error that may be swallowed and retried.

**4. Wrong model ID**
If the model string was changed to something invalid, the API returns a 400 that gets retried until timeout.

## How to diagnose quickly

Run this from the story-refinery directory:

```bash
node -e "
const Anthropic = require('@anthropic-ai/sdk');
const client = new Anthropic();
client.messages.create({
  model: 'claude-sonnet-4-6',
  max_tokens: 10,
  messages: [{ role: 'user', content: 'say hi' }]
}).then(r => console.log('OK:', r.content[0].text))
  .catch(e => console.error('FAIL:', e.message));
"
```

If that fails, it's an API key/network issue. If it succeeds, the problem is in `steps.ts` (likely non-streaming on large input or error swallowing).

To let me fix it directly: open a new Claude Code session from `/Users/deniswane/Claude/Neo/workspaces/personal/story-refinery` so I can read and edit the source files.
