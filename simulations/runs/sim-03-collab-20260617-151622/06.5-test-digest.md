There is no content to summarize. Both inputs — acceptance criteria and raw test specifications — contain only pipeline failure messages:

- **Acceptance criteria (`05-acceptance-criteria.md`):** Populated entirely with timeout errors across 4 batches × 6 retries each. No AC was ever generated.
- **Raw test specifications:** Contains a diagnosis message and a proposed fix, not Gherkin. The test generator was never able to produce output because it received no valid AC as input.

The pipeline stalled at the AC-writer step. The test coverage digest cannot be produced until the upstream stages complete successfully.

---

The diagnosis in your test spec input is accurate. To unblock the pipeline:

1. In `run-simulation.sh`, find and update:
   - `CALL_TIMEOUT` from `180` to `360`
   - `BATCH_TARGET_BYTES` from `8192` to `4096`

Would you like me to locate those lines and apply the edits?
