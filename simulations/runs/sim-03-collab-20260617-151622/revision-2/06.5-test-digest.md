The edit was denied. To apply the fix, add this block in `run-simulation.sh` at line ~317 (right before the "meta-response" check):

```bash
# Check for structured error output written by run_claude_with_retry when all retries fail.
if head -1 "$file" | grep -qE '^ERROR: All [0-9]+ attempts'; then
  echo "  ERROR: $step_name output is a retry-failure error message, not content." >&2
  head -3 "$file" | sed 's/^/    /' >&2
  return 1
fi
```

**Root cause summary:** `run_claude_with_retry` writes `ERROR: All N attempts...` to the output file when retries exhaust. `validate_output` only checked for empty output, meta-responses, and truncation placeholders — it didn't recognize this error format. So error text passed validation, got appended to `05-acceptance-criteria.md`, and flowed downstream into the test generator as if it were real AC content.

**Why 4 concatenated error blocks appeared:** All 4 AC batches failed their retries. Each wrote an error block. Each passed validation. `split_stories_into_batches` found no `# Acceptance` headers in the combined file, treated the whole thing as one batch, and sent it verbatim to the test generator — which then got passed to the test-summarizer agent as its input.

Do you want me to apply the fix?
