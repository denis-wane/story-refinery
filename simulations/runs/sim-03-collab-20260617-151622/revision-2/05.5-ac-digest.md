I need your permission to edit `run-simulation.sh`. Once you grant it, I'll make these two fixes:

**Fix 1** — `run-simulation.sh:374`: Change `CALL_TIMEOUT=180` → `CALL_TIMEOUT=600`

The AC writer batches are 29–32KB prompts generating thousands of lines — they need at least 5–10 minutes, not 3. Every batch hit the wall every single attempt.

**Fix 2** — `run-simulation.sh:536–577` (the AC writer loop): Add a skip guard after line 540:
```bash
if [ "$BATCH_STORY_COUNT" -eq 0 ]; then
  echo "  [Batch $b/$NUM_BATCHES] No story blocks (### headers) — skipping metadata batch"
  rm -f "$BATCH_FILE"
  continue
fi
```

The stories file has 4 trailing `## ` sections with zero `### ` story blocks (`Dependency Map`, `Suggested Implementation Order`, `Potential Additions`, `Coverage Check`). The splitter packs them into their own batch and sends it to the AC writer, which correctly reports "no stories here." The same guard is needed in the revision loop (`run-simulation.sh:760–806`).

Both the initial AC step and the revision-loop AC step have this issue, so the fix applies in two places.
